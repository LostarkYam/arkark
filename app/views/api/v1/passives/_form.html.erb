<%= form_with(model: passife, local: true) do |form| %>
  <% if passife.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(passife.errors.count, "error") %> prohibited this passife from being saved:</h2>

      <ul>
      <% passife.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
